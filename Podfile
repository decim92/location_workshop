# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Location Workshop' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Location Workshop

  pod 'GoogleMaps'
  pod 'Alamofire'
  pod 'SwiftyJSON'

  target 'Location WorkshopTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Location WorkshopUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
